'use babel';

import TreeViewFocusEditorView from '../lib/tree-view-focus-editor-view';

describe('TreeViewFocusEditorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
